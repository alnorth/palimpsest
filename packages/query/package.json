{
  "name": "palimpsest-query",
  "version": "0.1.0",
  "type": "module",
  "description": "Shared read query engine (filter/sort/paginate/resolve/serialize) for palimpsest UI surfaces",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "palimpsest": "*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsup": "^8.0.0",
    "typescript": "^6.0.0",
    "vitest": "^4.1.9"
  }
}
