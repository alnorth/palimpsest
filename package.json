{
  "name": "palimpsest-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace=packages/core",
    "typecheck": "npm run typecheck --workspaces"
  }
}
