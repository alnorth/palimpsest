{
  "name": "palimpsest-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces",
    "dev": "npm run dev --workspace=packages/mcp",
    "deploy": "npm run build --workspace=packages/backend && npm run deploy --workspace=packages/cdk",
    "deploy:ci": "npm run build --workspace=packages/backend && npm run deploy:ci --workspace=packages/cdk"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "4.62.2"
  }
}
